# 错题本

> 记录每次章节复习中的错题和半对题，方便考前快速回顾。
> 格式：每章一个二级标题，每题包含题目、我的答案、正确答案、掌握度。

---

## 章节：双存储架构 SQLite + ChromaDB（2026-05-28）

### 最终评定：🟢 通关（14/15 题正确或半对）

---

### ❌ 错题1：两条绳子的底层原因

**题目**: `metadatas` 里存 `{"document_id": 1}` 而不是解析 `ids` 字符串 `"doc1_chunk0"`，除了"方便"还有什么底层原因？

**我的答案**: 只记得和数据库有联系，其余忘了

**正确答案**: 数据库索引是 B+ 树结构，数字比较是 `O(1)` 直接比大小，字符串要逐字符比较 `O(n)`，且字符串索引占用更多存储。`document_id` 是整数主键，查询速度远快于字符串解析。

**掌握度**: ⭐⭐⭐⭐（2026-06-06 复习通过）

**一句话总结**: 数字比字符串查得快，这是数据库底层决定的。

---

### ⚠️ 半对题1：overlap 的作用（第一轮）

**题目**: `split_text(chunk_size=80, overlap=20)` 为什么要设置 overlap？不重叠直接切不行吗？

**我的答案**: 我不明白

**正确答案**: `overlap` 防止句子在切片边界被切断。例："我喜欢吃苹果"切成"我喜欢吃"和"苹果"就断了语义。重叠 20 字保证上下文连贯。但 overlap **不能消灭**边界断句，只能减少，Embedding 模型靠上下文词汇理解语义。

**掌握度**: ⭐⭐ → ⭐⭐⭐⭐（第三轮已掌握）

**一句话总结**: overlap 是用空间换连贯性，越大越连贯但存储翻倍。

---

### ⚠️ 半对题2：include 参数默认值

**题目**: `collection.query(include=["metadatas", "distances"])` 里，如果不写 `"metadatas"` 会发生什么？

**我的答案**: 不写就返回默认的属性

**正确答案**: `ids` 和 `distances` 总是返回，但 `metadatas`/`documents`/`embeddings` 默认**不返回**！不写 `"metadatas"` 就拿不到 `document_id`，后续回查 SQLite 时 `chroma_results["metadatas"]` 直接 KeyError 报错。

**掌握度**: ⭐⭐ → ⭐⭐⭐⭐（第三轮已掌握）

**一句话总结**: metadatas 不是默认返回的，不写就报错。

---

### ⚠️ 半对题3：`documents` 参数的必要性

**题目**: `collection.add()` 的 `documents` 参数存的是碎片文本，`metadatas` 里也有 `document_id`。那 `documents` 是不是多余的？能不能去掉？

**我的答案**: 不能去掉，有存在的道理，但不清楚为什么

**正确答案**: `documents` 不是多余的。单用 ChromaDB 时（不搭 SQLite），`documents` 是唯一文本来源，查询时直接返回展示。双存储架构里虽然从 SQLite 取完整内容，但 `documents` 存碎片文本可用于快速预览。去掉后单库场景无法工作。

**掌握度**: ⭐⭐⭐⭐（2026-06-06 复习通过）

**一句话总结**: documents 让 ChromaDB 能独立工作，不依赖 SQLite。

---

### ✅ 已掌握知识点（快速回顾）

| 知识点 | 我的理解 | 状态 |
|--------|---------|:---:|
| 双存储为什么存在 | ChromaDB 碎片太小，需要 SQLite 存全文 | ✅ |
| 写入顺序 | 先 SQLite 后 ChromaDB，保证数据一致性 | ✅ |
| 失败影响 | SQLite 失败→查不到原文；ChromaDB 失败→搜不到 | ✅ |
| 余弦距离转换 | `similarity = 1 - distance` = `cos(θ)` | ✅ |
| 查询流程 | ChromaDB 检索 → metadatas 拿 doc_id → SQLite 回查 | ✅ |
| overlap 作用 | 减少边界断句，用空间换连贯性 | ✅ |
| 边界断句现象 | 固有特性，Embedding 靠上下文理解 | ✅ |

---

## 章节：LangChain 底层原理与 Python 基础补漏（2026-06-05）

### 预检盲区（今日追问暴露）

| 盲区 | 今日表现 | 预计风险 |
|------|---------|---------|
| `global` 关键字 | 反复追问"global 是让变量全局可用吗" | 单例/缓存模式理解不透 |
| 抽象基类（ABC） | 混淆"继承为了复用代码"vs"继承为了接口契约" | 无法辨别抽象类和普通类 |
| 单例模式必要性 | 反复问"为什么不直接实例化" | 设计模式决策能力不足 |

---

### 第一轮测试记录（基础概念）

**得分**: 3.5/5（70%）— 🟡 补漏

#### ❌ 错题1：`embed_query` vs `embed_documents` 混淆

**题目**: `similarity_search` 的调用链顺序是什么？
**我的答案**: D（用了 `embed_documents`）
**正确答案**: A（`similarity_search` → `embed_query` → `get_embedding` → `get_embedding_model` → `encode`）
**错误原因**: 混淆了查询和存储的 Embedding 方法。查询用 `embed_query`，存文档用 `embed_documents`。
**掌握度**: ⭐⭐⭐⭐（2026-06-06 复习通过）

**一句话总结**: `similarity_search` 是查，走 `embed_query`；`add_documents` 是存，走 `embed_documents`。

#### ⚠️ 半对题1：设计模式术语不准

**题目**: `get_embedding_model()`、`get_langchain_embeddings()`、`get_vectorstore()` 共同用了什么设计模式？
**我的答案**: 懒加载 + 单一返回值模式
**正确答案**: 单例模式（Singleton）+ 惰性加载（Lazy Loading）
**错误原因**: "懒加载"对了，但"单一返回值模式"不是标准术语。
**掌握度**: ⭐⭐⭐⭐（2026-06-06 复习通过）

**一句话总结**: 标准名称是"单例模式"——全局只有一个实例，首次调用时才创建。

---

### 第二轮测试记录（细节深挖）

**得分**: 3.5/5（70%）— 🟡 补漏

#### ❌ 错题2：`global` 只读场景

**题目**: 函数中只读取全局变量（不赋值），需要写 `global` 吗？
**我的答案**: A（必须写）
**正确答案**: B（不需要写）
**错误原因**: `global` 只在**修改**全局变量时才需要。只读不写。
**掌握度**: ⭐⭐⭐⭐（2026-06-06 复习通过）

**一句话总结**: `global` 是"修改许可证"，不是"阅读许可证"。

#### ⚠️ 半对题2：完整调用链边界不清

**题目**: 从 HTTP 请求到 `SentenceTransformer.encode()` 的调用链
**我的答案**: 漏了 `similarity_search` 和 `model.encode`，多写了 `_generate_stream`、`_build_rag_chain`
**正确答案**: `路由 → get_vectorstore → similarity_search → get_langchain_embeddings → embed_query → get_embedding → get_embedding_model → model.encode`
**错误原因**: 把**生成答案**的调用链（`_build_rag_chain`、`_generate_stream`）和**检索向量**的调用链混在一起了。
**掌握度**: ⭐⭐⭐⭐（2026-06-06 复习通过）

**一句话总结**: 检索阶段到 `model.encode()` 就结束，后面的 `_build_rag_chain` 属于生成阶段，是另一条链。

---

### 使用说明

### 复习节奏

1. **当天**: 答完题立即记录错题，用一句话总结正确答案
2. **3天后**: 快速过一遍本章错题，遮住正确答案自测

---

## 章节：上下文窗口管理（2026-06-03）

### 最终评定：🟢 通关（4/5 题正确）

---

### ❌ 错题1：资料片段超限时的截断策略

**题目**: 在 `build_system_prompt` 中，当累积 Token 加上下一个片段会超过 `MAX_CONTEXT_TOKENS` 时，代码应该怎么做？

**我的答案**: B（跳过当前片段，继续尝试塞入下一个更短的片段）

**正确答案**: C（直接 `break` 退出循环，不再塞入更多片段）。因为 `results` 已按相似度从高到低排序，最相关的都塞不下，后面的更不重要且可能也塞不下。`break` 是安全且高效的做法。

**掌握度**: ⭐⭐⭐⭐（2026-06-06 复习通过）

**一句话总结**: 行李箱满了就拉拉链走人，不要跳过羽绒服去翻袜子。

---

### ✅ 已掌握知识点（快速回顾）

| 知识点 | 我的理解 | 状态 |
|--------|---------|:---:|
| 上下文窗口本质 | LLM 的短期记忆容量，有限且需管理 | ✅ |
| 三层防御 | MAX_CONTEXT / MAX_INPUT / MAX_OUTPUT 各司其职 | ✅ |
| 为什么不能统一 | 统一会导致某一层挤爆另一层 | ✅ |
| 总输入超限处理 | yield SSE 错误消息，不是 return 字符串 | ✅ |
| Token 估算工具 | tiktoken(cl100k_base)，Embedding 不能算 Token | ✅ |
3. **1周后**: 只看 ❌ 和 ⭐⭐ 的题，已掌握的跳过
4. **考前/进入下一章前**: 全扫一遍，确保没有遗留盲区

### 标记规范

| 标记 | 含义 |
|------|------|
| ❌ | 完全答错 |
| ⚠️ | 半对（核心对但细节缺） |
| ✅ | 完全正确 |
| ⭐ | 掌握度（1~5星） |

### 新增章节模板

```markdown
## 章节：[章节名]（YYYY-MM-DD）

### 最终评定：[🟢通关/🟡补漏/🔴重修]

---

### ❌/⚠️ 错题N：[题目简述]

**题目**: [原题]
**我的答案**: [我的回答]
**正确答案**: [标准答案]
**掌握度**: [⭐数]
**一句话总结**: [用自己的话概括]

---

### ✅ 已掌握知识点（快速回顾）

| 知识点 | 我的理解 | 状态 |
|--------|---------|:---:|
| ... | ... | ✅ |

---

## 章节：RAG 闭环 检索到回答（2026-05-31）

### 最终评定：🟡 补漏（3.5/5 题正确或半对）

---

### ⚠️ 半对题1：`enumerate(start=1)` 的作用

**题目**: `build_system_prompt` 里 `for i, r in enumerate(results, start=1)` 中的 `i` 是干什么的？为什么要格式化成 `[{i}]`？

**我的答案**: 知道 `title` 是来源、`similarity` 是余弦相似度、`chunk_content` 是数据切片，但不知道 `i` 的作用，猜测是让 LLM 回答末尾附上这些数据

**正确答案**: `i` 是片段编号（1, 2, 3...），由 `enumerate(start=1)` 自动生成。`[{i}]` 让 LLM 能引用"第几条资料"（如"根据[1]..."），用户也能对应上 LLM 说的是哪条资料。没有编号，资料混在一起 LLM 分不清。

**掌握度**: ⭐⭐ → 需复习

**一句话总结**: `[1]` `[2]` 是给资料贴编号，LLM 和用户都能精确引用。

---

### ⚠️ 半对题2：`yield` 的暂停机制

**题目**: `generate_rag_stream` 里为什么要用 `yield` 而不是 `return`？

**我的答案**: 记得 `return` 只返回一次，`yield` 是步进器可以流式返回，但原理忘记了

**正确答案**: `yield` 会**暂停**函数执行并保存当前状态，下次从暂停处继续。配合 `StreamingResponse` 实现 SSE 流式传输，LLM 每生成一个字就通过 HTTP 推送给浏览器，用户实时看到"打字机效果"。`return` 必须等全部回答生成完才能一次性返回，用户要等几秒到几十秒。

**掌握度**: ⭐⭐ → 需复习

**一句话总结**: `yield` = 说一个字给一个字，`return` = 全部说完再一起给。

---

### ✅ 已掌握知识点

| 知识点 | 我的理解 | 状态 |
|--------|---------|:---:|
| RAG 完整链路 | 向量化 → ChromaDB → SQLite → Prompt → LLM | ✅ |
| Prompt 格式设计 | `[1]` 编号、`来源`、 `第N段`、`相关度` 都是人为设计的 | ✅ |
| BaseModel vs Base | Pydantic 管数据校验，SQLAlchemy 管数据库映射 | ✅ |
| SQLite 回查逻辑 | doc_id/chunk_index 来自 ChromaDB metadata，查 Document 取 title | ✅ |
| yield vs return | yield 多次返回，return 一次返回 | ✅ |

---

### ⚠️ 半对题3：`ContentStream` 是类还是类型别名（2026-06-02 补充）

**题目**: `StreamingResponse.__init__` 中 `content: ContentStream` 的 `ContentStream` 是什么？是类吗？

**我的答案**: 一度以为是类，后理解为 TS 联合类型，但不知道 Python 里叫什么，也不清楚它和实际传入的 `async_generator` 对象的关系

**正确答案**: `ContentStream` 是**类型别名**（Type Alias），不是类。`ContentStream = Union[AsyncContentStream, SyncContentStream]` 等价于 TS 的 `AsyncIterable | Iterable`。运行时完全不存在，只是给 IDE 看的提示。实际传入的是 `async_generator` 对象，它"符合" ContentStream 的描述。

**掌握度**: ⭐⭐⭐ → 已掌握

**一句话总结**: `ContentStream` 是类型别名（类似 TS 联合类型），不是类；运行时无意义，只给 IDE 看。

---

### ⚠️ 半对题4：类 vs 类型 vs 类型提示 辨析（2026-06-02 补充）

**题目**: Python 中"类"、"类型"、"类型提示"有什么区别？

**我的答案**: 混淆了三者，以为 ContentStream 是一个类或类型

**正确答案**: 在 Python 中，"类"和"类型"本质一样（`class Dog` 创建类，`type(dog)` 返回类型 `<class 'Dog'>`），而"类型提示"（`: str`、`: ContentStream`）只是给 IDE 看的注释，运行时完全不存在。

**掌握度**: ⭐⭐⭐ → 已掌握

**一句话总结**: 类 = 类型（Python 里一样），类型提示 = 注释（运行时不存在）。
```
