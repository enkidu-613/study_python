### 2026-07-03 00:19 - RAG Chunking - page_content 才是被向量化文本

- id: lq-20260703-0019-page-content-embedding
- chapter: md/23_RAG_Chunking策略.md
- question: 在 page_content 和 metadata 里，什么代表被向量化后的文本？
- answer_summary: `page_content=chunk_text` 代表送进 Embedding 模型、被转换成向量的原始文本。`metadata` 是跟随这段文本保存的标签信息，例如文档 ID、chunk 序号、标题和来源，主要用于过滤、回查和展示，不是默认拿来做语义向量的正文。
- exact_rule: 被向量化的是 `page_content` / `document` 对应的文本；`metadata` 是标签，不是正文向量本体。
- reinforce_prompt: Chroma/LangChain 里 `page_content` 和 `metadata` 分别负责什么？
- status: active

### 2026-07-20 00:53 - LangGraph - add_edge 与 add_conditional_edges

- id: lq-20260720-0053-langgraph-edge-method-comparison
- chapter: md/29_LangGraph状态工作流.md
- question: `add_conditional_edges` 和普通 `add_edge` 的区别是什么？
- answer_summary: `add_edge(start, end)` 注册固定路线，无论 state 如何都从 start 进入指定的 end。`add_conditional_edges(source, path)` 注册动态路线，运行到 source 后先调用 path 路由函数，根据它返回的节点名决定下一步。前者注册时知道终点，后者要等 invoke 运行时才知道终点。
- exact_rule: `add_edge` 固定下一步；`add_conditional_edges` 先判断再选下一步。
- reinforce_prompt: 如果模型有和没有 `tool_call`，应该使用哪种边来选择 `tools` 或 END？
- status: active

### 2026-07-20 00:51 - LangGraph - 条件边与路由函数

- id: lq-20260720-0051-langgraph-conditional-edge-router
- chapter: md/29_LangGraph状态工作流.md
- question: 最后没有手写 `model -> END`，是不是因为用了 `add_conditional_edges`？
- answer_summary: 是，但要分清两层：`add_conditional_edges("model", tools_condition)` 负责注册“根据判断结果选择下一条边”的条件边；`tools_condition` 负责检查最后一条消息并返回 `"tools"` 或内部结束标签 `"__end__"`。两者配合后，才会在没有工具调用时自动走 END。
- exact_rule: `add_conditional_edges` 提供动态分支，路由函数返回目的地；方法本身不替你判断业务条件。
- reinforce_prompt: `add_conditional_edges` 和 `tools_condition` 各自负责什么？
- status: active

### 2026-07-20 00:50 - LangGraph - START 不参与工具循环

- id: lq-20260720-0050-langgraph-start-loop-boundary
- chapter: md/29_LangGraph状态工作流.md
- question: `tools -> model` 回边后是否会回到 `START`？最后一个 `model` 如何结束？
- answer_summary: `START` 只在一次 `graph.invoke(...)` 开始时经过一次，不参与循环。工具执行后通过 `tools -> model` 回到模型；这个新的模型响应完成后再次运行 `tools_condition`。没有新的 `tool_call` 就去 `END`，还有调用就再次去 `tools`。
- exact_rule: `START` 是一次执行的入口，不是循环节点；`tools -> model -> tools_condition` 才是工具循环，最终由条件函数路由到 `END`。
- reinforce_prompt: 工具返回后，下一步为什么是 `model` 而不是 `START`？最后一个 `model` 之后谁决定结束？
- status: active

### 2026-07-20 00:47 - LangGraph - tools_condition 路由函数

- id: lq-20260720-0047-langgraph-tools-condition-router
- chapter: md/29_LangGraph状态工作流.md
- question: `tools_condition` 是什么？
- answer_summary: `tools_condition` 是 LangGraph 提供的普通路由函数，接收当前 state，检查 `state["messages"]` 最后一条 AIMessage 是否有 `tool_calls`。有调用时返回 `"tools"`，没有调用时返回内部结束标签 `"__end__"`，从而让条件边去 ToolNode 或 END。它不执行模型、不执行工具，也不负责修改 State。
- exact_rule: `tools_condition` 只判断下一步去哪：有 `tool_calls` 去 `tools`，没有去 `END`。
- reinforce_prompt: `tools_condition` 的输入是什么？它返回哪两个关键目的地？
- status: active

### 2026-07-20 00:43 - LangGraph - bind_tools 与 ToolNode 的分工

- id: lq-20260720-0043-langgraph-bind-tools-tool-node-boundary
- chapter: md/29_LangGraph状态工作流.md
- question: LangGraph 已经有 `ToolNode`，为什么还要使用 `llm.bind_tools(tools)`？
- answer_summary: 两者不重复：`llm.bind_tools(tools)` 把工具的 schema 交给模型，让模型知道工具名称、参数和用途，并能提出 `tool_call`；`ToolNode(tools)` 接收模型提出的调用，找到真实 Python 工具并执行，再把结果写回 State。前者负责“模型提出调用”，后者负责“系统落实调用”。
- exact_rule: `bind_tools` 给模型看工具说明，`ToolNode` 执行真实工具；同一个 `tools` 列表要分别交给模型和执行器。
- reinforce_prompt: 没有 `bind_tools` 或没有 `ToolNode` 时，工具调用链分别会缺哪一步？
- status: active

### 2026-07-20 00:27 - LangGraph - create_agent 与 ChatDeepSeek 的层级

- id: lq-20260720-0027-agent-model-factory-layers
- chapter: md/29_LangGraph状态工作流.md
- question: 这里到底是 `create_agent`，还是 `ChatDeepSeek` 创建的模型类？
- answer_summary: `ChatDeepSeek` 是模型类，`ChatDeepSeek(...)` 创建模型实例 `llm`；`create_agent(...)` 是工厂函数，接收模型和工具并返回封装了调用与工具循环的 Agent。第六关的 `model_with_tools = llm.bind_tools(tools)` 是模型实例绑定工具后的对象，不等于 `create_agent`。
- exact_rule: `ChatDeepSeek(...)` 创建模型，`create_agent(...)` 创建 Agent，`llm.bind_tools(tools)` 创建绑定工具后的模型对象。
- reinforce_prompt: `llm`、`model_with_tools` 和 `agent` 分别由哪段代码创建？谁负责工具循环？
- status: active

### 2026-07-20 00:26 - LangGraph - model_with_tools 与 call_model

- id: lq-20260720-0026-langgraph-model-with-tools-name
- chapter: md/29_LangGraph状态工作流.md
- question: 第六关 `call_model` 里的 `call_with_tools` 是什么？
- answer_summary: 当前第六关实际写的是 `model_with_tools`，不是 `call_with_tools`。`model_with_tools` 是 `llm.bind_tools(tools)` 返回的、已经知道工具 schema 的模型对象；`call_model` 才是普通 Python 函数，它读取 state 并调用 `model_with_tools.invoke(...)`。`call_with_tools` 如果出现在自己的代码里，只可能是自定义变量名，不是 LangGraph 固定 API。
- exact_rule: `model_with_tools` 是绑定工具后的模型对象，`call_model` 是调用它的 Node 函数；不要把变量名误认为框架函数。
- reinforce_prompt: `model_with_tools = llm.bind_tools(tools)` 创建了什么？`call_model` 又负责什么？
- status: active

### 2026-07-19 23:43 - LangGraph - Checkpointer 保存的历史是什么

- id: lq-20260719-2343-langgraph-checkpointer-state-history
- chapter: md/29_LangGraph状态工作流.md
- question: `checkpointer` 是否让 LangGraph 获得了快照和历史保存能力？
- answer_summary: 是，但准确说它保存的是图执行过程中的 `State` 快照链，不是自动保存任意聊天历史。只有 `State` 中包含 `messages` 字段时，消息才会随快照保存；`InMemorySaver` 只在当前 Python 进程中保存，重启后会丢失。跨进程或重启保留需要数据库等持久化 checkpointer。
- exact_rule: Checkpointer 保存/恢复 State 快照；`messages` 只是可能被保存的 State 字段；`InMemorySaver` 不等于永久数据库。
- reinforce_prompt: 如果 State 没有 `messages` 字段，checkpointer 会不会自动生成聊天历史？重启 InMemorySaver 后数据还在吗？
- status: active

### 2026-07-19 23:16 - LangGraph - compile 的 checkpointer 参数

- id: lq-20260719-2316-langgraph-compile-checkpointer-argument
- chapter: md/29_LangGraph状态工作流.md
- question: `graph = builder.compile(checkpointer=InMemorySaver())` 里的参数是什么？
- answer_summary: `checkpointer` 是 `compile()` 的参数名，`InMemorySaver()` 是传入的参数值表达式，它会创建一个内存检查点保存器实例。这个保存器用于保存和恢复图执行过程中的 state 快照；`compile()` 本身不执行 Node，真正运行仍由 `graph.invoke(...)` 完成，并且启用它后要提供 `thread_id`。
- exact_rule: 参数名是 `checkpointer`，参数值是 `InMemorySaver()` 创建的保存器实例；它为编译后的图接入 state 保存/恢复能力。
- reinforce_prompt: `checkpointer=saver` 中哪个是参数名、哪个是参数值？为什么 invoke 还需要 `thread_id`？
- status: active

### 2026-07-19 22:45 - LangGraph - compile 与 invoke

- id: lq-20260719-2245-langgraph-compiled-state-graph
- chapter: md/29_LangGraph状态工作流.md
- question: `builder.compile()` 是否是把上面的步骤打包成蓝图，`invoke()` 是否负责执行这个蓝图？
- answer_summary: 方向正确，但专业术语要更准确：`builder` 是构建中的状态图/图纸，`compile()` 会检查并生成可运行的“编译后的状态图”（compiled state graph）；当前项目版本的具体类型是 `CompiledStateGraph`。`invoke()` 才会传入初始 state，沿着 Edge 执行 Node，并返回最终 state。
- exact_rule: `builder` 负责构建，`compile()` 生成可运行的 `CompiledStateGraph`，`invoke()` 执行它。
- reinforce_prompt: `compile()` 会执行 Node 吗？真正执行 Node 的方法是什么？
- status: active

### 2026-07-19 22:43 - LangGraph - START 与 END 的含义

- id: lq-20260719-2243-langgraph-start-end-markers
- chapter: md/29_LangGraph状态工作流.md
- question: `builder.add_edge("create_answer", END)` 和 `builder.add_edge(START, "normalize_question")` 分别是什么意思？
- answer_summary: `START` 和 `END` 是 LangGraph 提供的特殊流程标记，不是普通业务节点名。前者表示图的入口，后者表示流程结束；中间的字符串必须是之前通过 `add_node` 注册的节点名称。`create_answer -> END` 表示该节点完成后不再进入后续节点。
- exact_rule: `START -> 节点名` 表示从入口开始执行该节点；`节点名 -> END` 表示该节点完成后结束流程。
- reinforce_prompt: `START`、普通节点名和 `END` 在一条 edge 中分别代表什么？
- status: active

### 2026-07-19 22:07 - LangGraph - Edge 是什么

- id: lq-20260719-2207-langgraph-edge-routing-rule
- chapter: md/29_LangGraph状态工作流.md
- question: 我知道 `state` 是 LangGraph 中流转的数据、节点是函数，那么边是什么？
- answer_summary: Edge 是节点之间的流程连接规则，不负责执行具体业务。`builder.add_edge("a", "b")` 只是在构建图时登记“a 完成后把 state 交给 b”；真正调用 `graph.invoke(...)` 时，LangGraph 才按这条边安排下一步。普通边固定下一步，条件边根据 state 或消息选择下一步。
- exact_rule: Node 负责做事，Edge 负责安排下一步，State 负责携带共享工作数据。
- reinforce_prompt: `builder.add_edge("a", "b")` 在构建阶段做了什么？在 `invoke` 阶段又如何生效？
- status: active

### 2026-07-19 01:44 - Fine-tuning - 微调改变的是什么

- id: lq-20260719-0144-fine-tuning-what-it-does
- chapter: 未归属
- question: 模型微调是干什么的？
- answer_summary: 微调是用一批特定示例继续训练已有模型，实际改变部分模型参数，使它更稳定地学会某类任务、格式或语气。它不是在每次提问时临时塞资料；知识更新和可引用的私有资料通常优先用 RAG，微调适合稳定改变输出行为。
- exact_rule: Prompt 是本次请求的临时说明，RAG 是本次请求临时补资料，Fine-tuning 是用训练数据长期改变模型参数。
- reinforce_prompt: 当公司知识库每天更新时，为什么通常不先选择微调？
- status: active

### 2026-07-19 01:42 - 学习路线 - AI 应用开发与模型训练的边界

- id: lq-20260719-0142-ai-app-vs-model-training
- chapter: 未归属
- question: 本项目教程是否属于 AI 应用开发工程师路线，其中是否混入了 AI 训练和算法岗内容？
- answer_summary: 本项目主线是 AI 应用开发：Python、FastAPI、RAG、Agent、评估、可观测性、部署和前后端整合，重点是把预训练模型做成可靠产品。Embedding 实验和 Hugging Face 章节使用或本地推理预训练模型，不训练模型；唯一明确相关的是标为选修的 Fine-tuning 基础，用来判断何时应微调及理解 LoRA/QLoRA，不是算法岗训练路线。
- exact_rule: AI 应用开发的主线是选模型、调 API、做 RAG/Agent、评估和上线；从零训练模型、推导算法、构建训练集群属于 ML/算法岗的另一条路线。
- reinforce_prompt: 为什么 Hugging Face 本地推理和 Embedding 实验不等于模型训练？
- status: active

### 2026-07-16 00:57 - LangGraph - 多次工具调用如何循环

- id: lq-20260716-0057-langgraph-multiple-tool-loop
- chapter: md/29_LangGraph状态工作流.md
- question: 如果模型需要多次调用工具，`tools_condition` 和 `builder.add_edge("tools", "model")` 会如何循环？
- answer_summary: 一次工具调用不是流程终点。模型响应包含 `tool_call` 时，`tools_condition` 把流程送到 `ToolNode`；工具结果写回 `messages` 后，通过 `tools -> model` 回到模型。模型可以再次提出新的 `tool_call`，直到某次响应没有新的 `tool_call`，才由 `tools_condition` 路由到 `END`。
- exact_rule: `tools -> model` 是循环回边；`model` 响应没有 `tool_call` 才结束，生产代码还要限制工具次数并校验参数。
- reinforce_prompt: 模型连续调用两个工具时，节点顺序分别是什么？什么时候才到 `END`？
- status: active

### 2026-07-16 - LangGraph - tools_condition 与最终回答

- id: lq-20260716-langgraph-tools-condition-answer
- chapter: md/29_LangGraph状态工作流.md
- question: `tools_condition` 判断走 END 后，模型正在输出的答案怎么办？
- answer_summary: `tools_condition` 不会在模型生成过程中监控或打断输出。它在 model node 完成一次调用并返回完整 AIMessage 后检查 `tool_calls`；没有 tool_call 时走 END，但普通回答已保存在 AIMessage 的 content 和 State/messages 中。流式输出只是逐步传输 token，路由仍在本次 model node 可检查消息后决定。
- exact_rule: `tools_condition` 负责路由，不负责生成或停止模型；`END` 结束的是图的后续执行，不会删除已经生成的回答。
- reinforce_prompt: `AIMessage(content="最终回答", tool_calls=[])` 为什么可以走 END？
- status: active

### 2026-07-16 - LangGraph - END 的位置

- id: lq-20260716-langgraph-end-location
- chapter: md/29_LangGraph状态工作流.md
- question: LangGraph Agent 图里的 `END` 应该写在哪里？
- answer_summary: 在本章使用 `tools_condition` 的写法中，不需要额外写 `builder.add_edge("model", END)`；`tools_condition` 在模型没有 tool_call 时把路径返回到 END，有 tool_call 时返回 tools。若自己写路由函数，则由路由函数返回 END 或在条件边映射中指向 END。
- exact_rule: `tools -> model` 是工具结果回流；`model -> END` 是模型没有新工具调用时的结束路径，由条件路由决定。
- reinforce_prompt: 为什么同时写普通的 `model -> END` 和 `model -> tools` 条件边可能破坏预期路由？
- status: active

### 2026-07-16 - LangGraph - tools 节点回到 model 节点

- id: lq-20260716-langgraph-tools-model-edge
- chapter: md/29_LangGraph状态工作流.md
- question: 为什么 `builder.add_edge("tools", "model")` 后面看起来没有继续写代码？
- answer_summary: 这条边不是流程终止，而是把工具结果送回模型继续处理。代码层面接下来应先 `compile()` 得到可运行图，再用 `graph.invoke(...)` 执行；第 29 章只是暂时停止在代码形状预览，不要求当前关完成真实 Agent 调用。
- exact_rule: `tools -> model` 表示工具结果回到模型；真正的结束路径是 `model -> END`，由 `tools_condition` 判断模型是否还提出新的 tool_call。
- reinforce_prompt: 如果把 `tools` 直接连接到 `END`，模型还有机会阅读工具结果并生成最终回答吗？
- status: active

### 2026-07-15 - Python - 中文逗号导致括号解析错误

- id: lq-20260715-python-chinese-comma
- chapter: md/29_LangGraph状态工作流.md
- question: 为什么 `graph.invoke(...)` 会提示 `"(" was not closed`？
- answer_summary: 参数之间误用了中文逗号 `，`，Python 只识别英文逗号 `,`。解析器无法正确识别参数结束位置，因此错误常指向前面的左括号，而不一定直接标记中文逗号。
- exact_rule: Python 代码中的逗号、括号等标点必须使用英文半角符号。
- reinforce_prompt: 下面两者哪个是 Python 代码中的有效参数分隔符：`，` 还是 `,`？
- status: active

### 2026-07-15 - LangGraph - Checkpointer 缺少 thread_id

- id: lq-20260715-langgraph-checkpointer-config
- chapter: md/29_LangGraph状态工作流.md
- question: 为什么配置了 `checkpointer` 后，`graph.invoke(...)` 报错要求 `thread_id`？
- answer_summary: `compile(checkpointer=...)` 后，图的每次执行都必须通过 `config` 指定要读写哪条线程。当前代码虽然创建了 `config`，但调用 `graph.invoke(...)` 时没有传入 `config=config`，所以 LangGraph 找不到 `thread_id`。
- exact_rule: `graph.invoke(input_state, config=config)`；`checkpointer` 负责保存快照，`thread_id` 负责标识快照所属线程。
- reinforce_prompt: 如果只传 `thread_id` 而没有配置 checkpointer，LangGraph 能否保存 State？
- status: active

### 2026-07-15 - LangGraph - 最小模板与第五关练习

- id: lq-20260715-langgraph-template-vs-practice
- chapter: md/29_LangGraph状态工作流.md
- question: 当前的 `app/lang_graph.py` 和教学文档第五关的练习是两个不同的吗？
- answer_summary: 是。当前文件主要是教学文档前面的最小模板，验证 `question -> normalized_question -> answer`；第五关包含跟写 Edge 的流程实验，以及换业务场景独立重写 `topic -> outline -> preview`。独立重写应使用新的 State、Node 和 Graph，避免与原示例混淆。
- exact_rule: 最小模板用于读懂和验证基础链路；第五关独立重写必须更换业务字段和节点，并实际运行检查最终 State。
- reinforce_prompt: 当前 `LearningState` 的三个字段和第五关 `StudyState` 的三个字段分别是什么？
- status: active

### 2026-07-15 - LangGraph - Edge 文档覆盖

- id: lq-20260715-langgraph-edge-coverage
- chapter: md/29_LangGraph状态工作流.md
- question: 我的文档里介绍 Edge 是什么了吗？
- answer_summary: 介绍了。Edge 是节点之间的连接规则，负责决定流程下一步运行哪个 Node；本章用 `add_edge(...)` 展示了固定顺序，并强调 Node 做事、Edge 安排下一步、State 传递数据。条件边暂留到后续学习。
- exact_rule: `builder.add_edge("A", "B")` 表示 A 执行完成后进入 B；Edge 不负责业务处理，也不负责保存 State 内容。
- reinforce_prompt: `builder.add_edge("normalize_question", "create_answer")` 和 `normalize_question` 函数分别负责什么？
- status: active

### 2026-07-15 - LangGraph - Node 文档覆盖

- id: lq-20260715-langgraph-node-coverage
- chapter: md/29_LangGraph状态工作流.md
- question: 我的文档里介绍 Node 是什么了吗？
- answer_summary: 介绍了。第 29 章把 Node 定义为普通 Python 函数：读取当前 state，返回本节点负责更新的字段；并展示了 `add_node("名称", 函数)` 的注册方式和 Node 与 Edge 的分工。当前只讲固定顺序的最小 Node，条件边、工具 Node、异步 Node 和并行 Node 留给后续章节。
- exact_rule: Node 做事并返回状态更新；Edge 决定接下来运行哪个 Node；State 传递共享工作数据。
- reinforce_prompt: `normalize_question` 这个 Node 读了哪个 State 字段，又返回了哪个更新字段？
- status: active

### 2026-07-13 15:15 - LangGraph - TypedDict 作为 State 合同

- id: lq-20260713-1515-typeddict-state-contract
- chapter: md/29_LangGraph状态工作流.md
- question: `class LearningState(TypedDict)` 中继承 `TypedDict` 起到了什么作用？
- answer_summary: 它把 `LearningState` 声明为“有固定字段类型的字典 Schema”，告诉 IDE、类型检查器和 LangGraph State 这个字典应包含 `question`、`normalized_question`、`answer`，且值都是 str。它不是普通 OOP 继承，主要不提供实例方法或运行时校验。
- exact_rule: `TypedDict` 负责静态类型合同；运行时仍是普通 dict，例如 `{"question": "..."}`。
- reinforce_prompt: 少传 `answer` 或给 `question` 传 int 时，TypedDict 在运行时会自动报错吗？
- status: active

### 2026-07-13 15:12 - LangGraph - TypedDict 拼写

- id: lq-20260713-1512-typeddict-spelling
- chapter: md/29_LangGraph状态工作流.md
- question: 为什么 `from typing_extensions import TypeDict` 会报 ImportError？
- answer_summary: `typing_extensions` 导出的是 `TypedDict`，不是 `TypeDict`。`TypedDict` 意为 typed dictionary，用来描述字典各字段的类型；导入名和继承时的类名都必须准确写为 `TypedDict`。
- exact_rule: `TypedDict` 的拼写是 `Typed` 加 `Dict`；`TypeDict` 不是 Python 标准库或 `typing_extensions` 提供的名称。
- reinforce_prompt: `TypedDict` 在 LangGraph 的 State 中描述的是什么？
- status: active

### 2026-07-13 14:28 - Python 类型提示 - typing 与 typing_extensions

- id: lq-20260713-1428-typing-extensions-boundary
- chapter: md/29_LangGraph状态工作流.md
- question: typing_extensions 是哪个依赖的 typing，它和 Python 标准库 typing 有什么关系？
- answer_summary: typing 是 Python 标准库模块；typing_extensions 是独立安装的第三方兼容/扩展包，用于把新版类型提示能力带给旧版 Python。它不是某个框架私有的 typing，LangGraph 等库依赖它以兼容多种 Python 版本。在 Python 3.12 中 TypedDict 两边都能导入，本章沿用官方的 typing_extensions 写法以保持跨版本兼容。
- exact_rule: 优先按项目和官方示例导入；typing 是标准库，typing_extensions 是兼容扩展包，二者提供相似类型提示但不是同一个模块。
- reinforce_prompt: 为什么 Python 3.12 已有 TypedDict，LangGraph 示例仍可能从 typing_extensions 导入？
- status: active

### 2026-07-10 09:59 - Python 基础 - dict 与对象为什么分开

- id: lq-20260710-0959-dict-vs-object-boundary
- chapter: md/28_LangChain_Agent工具调用.md
- question: dict 和对象都像键值数据，为什么 Python 不把它们合并，何时该用 `[]` 或 `.`？
- answer_summary: dict 是动态映射，适合 JSON、配置和任意 hashable key；对象是类的实例，除属性外还能提供方法、类型约束、继承和属性访问逻辑。对象内部可能有 __dict__，但属性查找还会经过类、父类、property 等规则，因此不等于普通 dict。遇到外部动态数据通常用 dict，遇到固定领域概念和行为通常用类或 Pydantic 模型。
- exact_rule: dict 用 `data["key"]`；对象用 `obj.attribute`；需要时用 Pydantic 的 `model_dump()` 把对象显式转为 dict。
- reinforce_prompt: 为什么 AIMessage 要用 `.content`，而 `result` 要用 `["messages"]`？
- status: active

### 2026-07-10 09:52 - LangChain Agent - 编号、字符串拼接与结果取值

- id: lq-20260710-0952-enumerate-join-ai-message-content
- chapter: md/28_LangChain_Agent工具调用.md
- question: `enumerate(..., start=1)`、`join(...)` 和 `result["messages"][-1].content` 分别怎样处理数据？
- answer_summary: enumerate 不改原列表，只在循环时产出 `(编号, 元素)`；指定 start=1 才从 1 编号，默认从 0。f-string 为每个 Document 生成一条字符串，join 再用分隔符把多条字符串拼成一个 str。result 是字典形式的 state，messages 是列表，最后一项是 AIMessage 对象，所以依次用 `[]`、`[]`、`.` 取值。
- exact_rule: dict 用 `[]` 按 key 取值，list 用 `[]` 按下标取值，对象用 `.` 读取属性。
- reinforce_prompt: 为什么 `result["messages"][-1]["content"]` 会报错，而 `.content` 不会？
- status: active

### 2026-07-10 08:47 - LangChain Agent - Checkpointer 与 Agent State

- id: lq-20260710-0847-checkpointer-agent-state
- chapter: md/28_LangChain_Agent工具调用.md
- question: Checkpointer 为什么不只是历史对话，它具体保存和恢复什么？
- answer_summary: Checkpointer 是 LangGraph 的状态持久化器，按 thread_id 保存 Agent state 在执行节点后的快照。messages 只是 state 里的一个字段；state 还会包含本轮工具调用、工具结果等工作过程。下一次 invoke 使用相同 thread_id 时，框架先恢复上次 state，再合并新输入继续执行，因此它能表现为短期对话记忆。
- exact_rule: checkpointer 保存的是 Agent state 的快照；历史消息只是 state 中最常用的一部分，thread_id 决定恢复哪条快照链。
- reinforce_prompt: 为什么说 messages 是 state 的一部分，而 checkpointer 不是单纯的消息列表？
- status: active

### 2026-07-10 08:53 - LangChain Agent - 没有 Checkpointer 时的调用边界

- id: lq-20260710-0853-no-checkpointer-new-state
- chapter: md/28_LangChain_Agent工具调用.md
- question: 不传 thread_id 且不配置 checkpointer 时，每次 Agent 对话是否都从新状态开始？
- answer_summary: 是。没有 checkpointer 就没有跨 invoke 的 state 保存和恢复，每次 invoke 只从本次传入的 messages 构造新 state。此时不能严格说创建了一条新 thread，因为根本没有被保存的线程；它只是一次彼此独立的 Agent 执行。只传 thread_id 但没有 checkpointer 也不会产生记忆；配置了 checkpointer 却不提供它要求的 thread_id 通常会报错。
- exact_rule: 无 checkpointer = 每次 invoke 都是独立的新 state；同一个 thread 的恢复必须同时具备 checkpointer 和相同 thread_id。
- reinforce_prompt: 为什么只传 thread_id 不能让 Agent 记住上一轮？
- status: active

### 2026-07-03 01:53 - RAG Chunking - 向量由 Chroma 持久化保存

- id: lq-20260703-0153-vector-storage-chroma
- chapter: md/23_RAG_Chunking策略.md
- question: 向量存储在哪里了？
- answer_summary: 在当前项目里，Chroma 使用 `chromadb.PersistentClient(path="./chroma_db")`，所以向量和向量索引由 Chroma 持久化到项目根目录的 `chroma_db/`。SQLite 里的 `DocumentChunk.embedding_id` 只是保存 Chroma 记录 ID，用来把业务数据库里的 chunk 和 Chroma 里的向量记录关联起来。
- exact_rule: 业务正文和 chunk 关系在 SQLite；向量和向量索引在 `chroma_db/`；两边靠 `embedding_id` / Chroma `id` 关联。
- reinforce_prompt: 当前项目里 SQLite 和 Chroma 分别保存 RAG 的哪部分数据？
- status: active

### 2026-07-03 04:04 - RAG Evaluation - 追问需要历史或检索 trace

- id: lq-20260703-0404-rag-followup-trace
- chapter: md/24_RAG_评估与指标.md
- question: 如果追问“它是从知识库检索到的还是模型编的、chunk 排第几、有没有更相关的被挤掉”，是否需要对话历史，当前代码支持吗？
- answer_summary: 如果追问里的“它”“这个 chunk”指上一轮回答，就需要对话历史或更准确地说需要保存上一轮 retrieval trace。当前 `/langchain-rag/search` 能返回 chunk 内容、similarity、document_id、chunk_index，可用于手工评估；但 `/langchain-rag/chat` 只流式返回答案和来源标题，不保存上一轮检索结果，也不接收 chat_history，所以不能自然追问上一轮证据链。
- exact_rule: 普通 RAG 问答需要 query；可追问的 RAG 评估需要保存上一轮 retrieved chunks、rank、score 和 answer 的 trace。
- reinforce_prompt: 为什么“这个 chunk 排第几？”不能只靠下一轮普通 query 稳定回答？
- status: active

### 2026-07-03 05:21 - RAG Evaluation - 上下文太脏的处理顺序

- id: lq-20260703-0521-dirty-context-fixes
- chapter: md/24_RAG_评估与指标.md
- question: RAG 检索上下文太脏应该如何解决？
- answer_summary: 上下文太脏通常是检索结果里混入了太多无关 chunk。先用 `/search` 看 top_k 结果，优先调小 `n_results`、改 chunk_size/chunk_overlap、用 metadata 过滤来源或权限；更进阶再做 rerank、相似度阈值过滤或 query rewrite。
- exact_rule: 上下文太脏先少拿和过滤，再考虑 rerank；不要直接让模型自己在一堆噪声里挑。
- reinforce_prompt: 如果 RAG context 里混入大量无关 chunk，你会按什么顺序排查？
- status: active

### 2026-07-03 07:17 - RAG Evaluation - retrieval trace 和对话历史的区别

- id: lq-20260703-0717-trace-vs-chat-history
- chapter: md/24_RAG_评估与指标.md
- question: retrieval trace 和对话历史有什么区别？
- answer_summary: 对话历史保存用户和 AI 说过什么，主要解决多轮聊天连续性；retrieval trace 保存某次 RAG 调用了什么 query、检索到哪些 chunk、rank/score 是多少、最终 context 和 answer 是什么，主要解决证据链追踪和评估。普通历史能回答“刚才聊到什么”，但不能稳定回答“第几个 chunk 支持了答案”。
- exact_rule: 对话历史管聊天上下文；retrieval trace 管 RAG 证据链。
- reinforce_prompt: 为什么有 chat_history 仍不一定知道上一轮答案来自哪个 chunk？
- status: active

### 2026-07-03 08:39 - AI Agents - Tool 不等于 MCP

- id: lq-20260703-0839-tool-vs-mcp
- chapter: md/25_AI_Agents基础.md
- question: Tool 是 MCP 吗？
- answer_summary: Tool 是泛概念，表示模型可请求应用侧调用的一段能力；MCP 是一种标准协议，用来把外部系统提供的 tools/resources/prompts 接给 AI 应用。MCP 可以提供 tool，但 tool 不一定来自 MCP，本地 Python 函数、OpenAI tools、LangChain tools 都可以是 tool。
- exact_rule: Tool 是能力；MCP 是把外部能力标准化接入模型应用的协议。
- reinforce_prompt: 为什么说 MCP 可以提供 tool，但 tool 不等于 MCP？
- status: active

### 2026-07-04 02:39 - RAG / Embedding - 本地 Embedding 模型封装是否已教学

- id: lq-20260704-0239-local-embedding-wrapper
- chapter: md/15_LangChain核心概念.md / md/25_AI_Agents基础.md
- question: `app/embedding.py` 是 AI 生成的，本地部署 Embedding 模型这类技术是否已经在教程里讲过？
- answer_summary: 教程已经讲过 Embedding、真实向量、本地 BGE/MPS 的概念，尤其在 RAG 和 LangChain 章节；但 `app/embedding.py` 的工程化封装更完整，包括硬件检测、模型选择、懒加载单例、线程锁、batch encode、失败 fallback、LangChain Embeddings 适配器。它更像后续可补的“本地 Embedding 模型工程化封装”小节，而不是当前 Agent 章节的核心。
- exact_rule: 概念已学过，工程封装还没系统学；先知道调用链，后面再专门补本地 Embedding 工程化。
- reinforce_prompt: `app/embedding.py` 里哪些部分属于本地 Embedding 的工程化封装？
- status: active

### 2026-07-04 02:42 - AI Engineer Roadmap - 本地 Embedding 工程化学习优先级

- id: lq-20260704-0242-local-embedding-priority
- chapter: md/25_AI_Agents基础.md
- question: 根据 roadmap.sh AI Engineer 流程，本地 Embedding 模型工程化封装现在需要学吗？
- answer_summary: 需要学，但不应在当前 Agent 章节深入。AI Engineer 主线需要掌握 Embeddings、Vector Databases、RAG、Deployment/Monitoring；当前阶段先会使用 embedding/vectorstore 能力，理解调用链即可，本地模型硬件检测、fallback、batch、线程锁等工程化细节更适合放到 Hugging Face、本地推理或部署优化阶段。
- exact_rule: 现在会用 embedding；后面再系统学本地 embedding 工程化。
- reinforce_prompt: 为什么本地 Embedding 工程化不是当前 Agent 章节的核心？
- status: active

### 2026-07-04 03:03 - AI Agents - Tool JSON 放置和实际调用时机

- id: lq-20260704-0303-tool-schema-placement
- chapter: md/25_AI_Agents基础.md
- question: AI Agents 基础这一章是否要写 tool JSON、应该放在哪里，实际调用是不是下一章才学？
- answer_summary: 当前章节先理解 Tool/Tool Schema/Tool Call 的边界；真正写 JSON schema 是在实现 Function Calling 请求时才需要。项目里可以先把工具函数放 `app/tools/knowledge_base.py`，等实现 Agent Loop 时再把 schema 放在同文件的 `SEARCH_KNOWLEDGE_BASE_SCHEMA` 或集中放到 `app/tools/registry.py`，供 `app/routers/agent.py` 引用。
- exact_rule: 工具函数先放 `app/tools`；tool schema 等接 Function Calling 时再写，最好和工具函数或工具注册表放一起。
- reinforce_prompt: 为什么现在不必急着给每个工具写 JSON schema？
- status: active

### 2026-07-04 03:06 - AI Agents - Tool Schema 为什么写在 Python 里

- id: lq-20260704-0306-tool-schema-in-python
- chapter: md/25_AI_Agents基础.md
- question: Tool JSON 不放 JSON 文件，为什么要放在 Python 里？
- answer_summary: Tool schema 虽然长得像 JSON，但在 Python 项目里通常先写成 dict，因为它不是给人单独读取的配置文件，而是运行时要和工具函数、函数映射、路由一起被 Python 代码引用的对象。放在 Python 里更容易和真实函数保持同步，也方便导入、测试、组合和注册；如果后面工具很多，才可以抽成 registry 或独立配置。
- exact_rule: Tool schema 是“给模型看的函数说明书”，但它通常由 Python 运行时代码发送给模型，所以先写成 Python dict 最顺手。
- reinforce_prompt: 为什么 tool schema 长得像 JSON，却不一定要放进 `.json` 文件？
- status: active

### 2026-07-04 03:12 - AI Agents - Registry 里的 TOOLS 列表

- id: lq-20260704-0312-tools-registry-list
- chapter: md/25_AI_Agents基础.md
- question: `registry.py` 里的 `TOOLS = [...]` 是什么意思？
- answer_summary: `TOOLS = [...]` 是工具 schema 的列表，也就是准备发给模型看的“可用工具说明书清单”。模型会根据这个列表判断自己能调用哪些工具、每个工具叫什么、需要哪些参数；真正执行函数的是后端通常另有 `TOOL_FUNCTIONS = {"工具名": 函数}` 这样的映射。
- exact_rule: `TOOLS` 给模型看；`TOOL_FUNCTIONS` 给后端执行用。
- reinforce_prompt: 为什么 Agent 项目里常常同时有 `TOOLS` 和 `TOOL_FUNCTIONS`？
- status: active

### 2026-07-04 03:19 - AI Agents - Tool Schema required 层级

- id: lq-20260704-0319-tool-schema-required-level
- chapter: md/25_AI_Agents基础.md
- question: Tool schema 里的 `required` 应该放在 JSON 的哪个层级？
- answer_summary: `required` 应该放在 `parameters` 这个 object schema 里面，和 `properties` 同级，用来声明哪些参数是必填。它不应该放进单个参数字段里；例如 `query` 和 `limit` 都写在 `properties`，但 `required: ["query"]` 表示只有 `query` 必填，`limit` 可选。
- exact_rule: `required` 和 `properties` 同级，都在 `parameters` 里面。
- reinforce_prompt: 如果 `limit` 是可选参数，`required` 应该写成什么？
- status: active

### 2026-07-04 03:33 - AI Agents - Tool Call 和 Function Calling 的关系

- id: lq-20260704-0333-tool-call-vs-function-calling
- chapter: md/25_AI_Agents基础.md
- question: 题目里的 tool call 是 function calling 吗？
- answer_summary: Function Calling 是一种让模型按工具 schema 生成调用意图的机制；Tool Call 是模型在这套机制里具体生成的一次“我要调用哪个工具、参数是什么”的请求。两者关系很近，但层级不同：Function Calling 是机制/能力，Tool Call 是一次调用请求本身。
- exact_rule: Function Calling 是机制；Tool Call 是模型生成的一次工具调用请求，还不是后端函数执行结果。
- reinforce_prompt: 为什么说 Tool Call 不是后端已经执行了函数？
- status: active

### 2026-07-04 03:36 - AI Agents - Tool Call 执行细节的学习边界

- id: lq-20260704-0336-tool-call-execution-boundary
- chapter: md/25_AI_Agents基础.md
- question: 现在还没学到 tool call 和 Function Calling 是如何执行的，对吗？
- answer_summary: 对，当前第 25 章只要求理解模型会根据 tool schema 生成 tool call，真正执行由后端完成；还不要求掌握具体 SDK 返回结构、如何解析 tool call、如何调用 `TOOL_FUNCTIONS`、如何把 tool output 再发回模型。那些属于下一章“Agent Loop 实现”或“Function Calling 实战”的内容。
- exact_rule: 本章会画 loop；下一章才写执行 loop。
- reinforce_prompt: 当前章节和下一章节在 Agent Loop 上的边界是什么？
- status: active

### 2026-07-04 03:41 - AI Agents - Function Calling 和 Tool Call 数据结构

- id: lq-20260704-0341-function-calling-vs-tool-call-structure
- chapter: md/25_AI_Agents基础.md
- question: Function Calling 是描述流程，还是模型吐出的 tool call 函数引用数据结构？
- answer_summary: Function Calling 更准确说是模型按工具 schema 生成结构化工具调用请求的能力/机制，也可以泛指这条调用流程。模型真正吐出来的那段包含工具名和参数的数据结构，叫 tool call；后端读取 tool call 后才去执行真实函数。
- exact_rule: Function Calling 是能力/机制/流程；tool call 是模型输出里的具体调用数据结构。
- reinforce_prompt: Function Calling 和 tool call 在层级上有什么区别？
- status: active

### 2026-07-04 04:05 - AI Agents / 安全 - Audit 审计

- id: lq-20260704-0405-audit-log
- chapter: md/25_AI_Agents基础.md / md/26_Function_Calling执行Loop.md
- question: audit 是什么？
- answer_summary: `audit` 在安全和后端系统里通常指审计/留痕，也就是记录谁在什么时候做了什么操作、操作对象是什么、结果是成功还是失败。它不是普通 debug 日志，而是为了事后追踪、合规、安全排查和责任归因，尤其适合高风险工具调用。
- exact_rule: audit = 安全留痕；记录“谁、何时、对什么、做了什么、结果如何”。
- reinforce_prompt: 高风险 Agent 工具为什么需要 audit log？
- status: active

### 2026-07-06 04:14 - Function Calling - 第五关模拟边界

- id: lq-20260706-0414-tool-call-mock-boundary
- chapter: md/26_Function_Calling执行Loop.md
- question: 第五关的最小工具执行函数是否只是模拟，模型其实没有拿来用？
- answer_summary: 第五关只练 `tool_call -> Python 函数执行结果` 这一段，默认 `tool_call` 已经存在，所以没有包含模型根据 `TOOLS` 生成 `tool_call` 的步骤。真实 Function Calling 要在请求模型时传 `tools=TOOLS`，读取返回的 `assistant_message.tool_calls`，执行工具后再把 `role="tool"` 的结果放回 messages。
- exact_rule: 第五关是后端执行器切片；第八关才是模型生成 tool_call 并回传 tool output 的完整 loop。
- reinforce_prompt: 为什么只有 `run_tool_call(tool_call)` 还不能算完整 Agent Loop？
- status: active

### 2026-07-06 04:25 - Function Calling - tool_call 对象访问

- id: lq-20260706-0425-tool-call-object-access
- chapter: md/26_Function_Calling执行Loop.md
- question: 为什么第五关用 `tool_call.function.name`，而不是 `tool_call.name` 或 `.get()`？
- answer_summary: 模型原始返回里一次工具调用的结构是 `tool_call -> function -> name/arguments`，工具名不在顶层，而在 `function` 子对象里。OpenAI Python SDK 会把原始 JSON 包装成类型对象，所以示例里用点号访问；只有先把它转成 dict，才适合用 `.get()`。
- exact_rule: SDK 对象用 `tool_call.function.name`；dict 才用 `tool_call["function"]["name"]` 或 `.get()`。
- reinforce_prompt: `tool_call.function.arguments` 为什么还要 `json.loads()`？
- status: active

### 2026-07-06 06:09 - Function Calling - 章节实战边界

- id: lq-20260706-0609-function-calling-chapter-boundary
- chapter: md/26_Function_Calling执行Loop.md
- question: 第 26 章是不是依旧不是最终实战，只是继续理解 Function Calling 原理？
- answer_summary: 第 26 章不是产品级最终实战，也不是只讲概念；它是手写最小执行 loop 的原理级实战切片。目标是看懂模型如何吐出 tool_call、后端如何执行工具、tool output 如何回到 messages；完整业务接口、权限、多工具编排和框架封装属于后续章节。
- exact_rule: 本章学手写 Function Calling 执行链路；后面才学 LangChain Agent/LangGraph 等框架级实战。
- reinforce_prompt: 第 26 章和后续 LangChain Agent 实战的边界是什么？
- status: active

### 2026-07-07 18:15 - LangChain Memory - History 和 Wrapper 的区别

- id: lq-20260707-1815-history-vs-runnable-wrapper
- chapter: md/27_LangChain对话记忆.md
- question: `InMemoryChatMessageHistory` 和 `RunnableWithMessageHistory` 的区别是什么？
- answer_summary: `InMemoryChatMessageHistory` 是保存某个 session 历史消息的容器，像一本聊天记录本；`RunnableWithMessageHistory` 是包在 chain 外面的执行包装器，负责根据 session_id 找到这本记录本、把历史注入 prompt、调用模型、再把新消息写回历史。一个负责存，一个负责自动使用。
- exact_rule: `InMemoryChatMessageHistory` 存消息；`RunnableWithMessageHistory` 调 chain 时自动读历史、塞历史、写历史。
- reinforce_prompt: 为什么只有 `InMemoryChatMessageHistory` 还不能自动让 chain 带上历史？
- status: active

### 2026-07-08 01:25 - LangChain Memory - invoke 是启动自动记忆流程

- id: lq-20260708-0125-invoke-starts-history-wrapper
- chapter: md/27_LangChain对话记忆.md
- question: `chain_with_history.invoke(...)` 是什么，既然说会自动保存，为什么还要调用它？
- answer_summary: 自动保存不是凭空发生，而是在调用 `chain_with_history.invoke(...)` 时发生。`chain_with_history` 是包了记忆管理的 chain，`invoke` 是启动这次执行：先按 session_id 读历史并塞进 prompt，再调用模型，最后把本轮用户输入和模型回答写回 history。
- exact_rule: `chain_with_history.invoke(...)` = 启动一次“读历史 -> 调模型 -> 写历史”的自动记忆流程。
- reinforce_prompt: 为什么说自动保存发生在 `invoke` 的执行过程中？
- status: active

### 2026-07-08 01:33 - LangChain Memory - question 和 history 变量对接

- id: lq-20260708-0133-memory-variable-wiring
- chapter: md/27_LangChain对话记忆.md
- question: `input_messages_key="question"`、`history_messages_key="history"`、`MessagesPlaceholder("history")`、`("human", "{question}")` 分别在做什么？
- answer_summary: 这四行分成两组：`question` 负责当前用户输入，`invoke({"question": ...})` 的值会进入 `("human", "{question}")`；`history` 负责历史消息，`RunnableWithMessageHistory` 找到的历史会交给 `history_messages_key="history"`，再插入 `MessagesPlaceholder("history")`。变量名本身不神秘，关键是前后必须对得上。
- exact_rule: `question` 对当前输入；`history` 对历史插槽；名字可以换，但 `input_messages_key` 必须对 prompt 的 `{...}`，`history_messages_key` 必须对 `MessagesPlaceholder`。
- reinforce_prompt: 如果把 `{question}` 改成 `{input}`，`input_messages_key` 应该怎么改？
- status: active

### 2026-07-08 01:46 - LangChain Memory - session_id 和 user_id 边界

- id: lq-20260708-0146-session-id-vs-user-id
- chapter: md/27_LangChain对话记忆.md
- question: 第六关里的 `session_id` 是会话 ID 还是用户 ID，怎么判断用户的新请求是不是新对话？
- answer_summary: `session_id` 更准确是会话 ID，不是单纯的用户 ID；用户 ID 表示这个人是谁，会话 ID 表示这个人的哪一段对话。是否新对话通常由应用决定：用户点新建对话就生成新 session_id，打开旧聊天或同一窗口继续就复用旧 session_id，后端只根据传来的 session_id 找对应历史。
- exact_rule: 同一聊天窗口复用同一 `session_id`；新建聊天窗口生成新的 `session_id`；真实结构常是 `user_id -> session_id -> messages`。
- reinforce_prompt: 为什么一个用户不能只用一个固定 `session_id` 表示所有聊天？
- status: active

### 2026-07-08 02:12 - LangChain Memory - llm 来源

- id: lq-20260708-0212-langchain-memory-llm-source
- chapter: md/27_LangChain对话记忆.md
- question: 本章最小可抄模板里的 `llm` 从哪里来？
- answer_summary: `llm` 不是 LangChain 自动生成的变量，而是需要先创建的 Chat 模型对象。本项目里可以用 `ChatDeepSeek(...)`，从 `.env` 读取 `MODEL_NAME`、`MODEL_API_URL`、`MODELSCOPE_API_KEY`，然后 `chain = prompt | llm` 才表示 prompt 的输出交给模型。
- exact_rule: `chain = prompt | llm` 之前必须先定义 `llm = ChatDeepSeek(...)`。
- reinforce_prompt: 为什么只写 `chain = prompt | llm` 但没有定义 `llm` 会让模板不可运行？
- status: active

### 2026-07-08 02:25 - LangChain Memory - HumanMessage 不是模板消息

- id: lq-20260708-0225-humanmessage-not-template
- chapter: md/27_LangChain对话记忆.md
- question: `HumanMessage(content="{question}")` 能不能作为 `ChatPromptTemplate.from_messages(...)` 里的占位符使用？
- answer_summary: 不推荐这样用；`HumanMessage(content="{question}")` 是已经创建好的消息对象，当前 LangChain 会把 `{question}` 当成普通文本，而不是模板变量。需要变量替换时应使用 `("human", "{question}")` 这种模板消息写法。
- exact_rule: 要变量替换，用 `("human", "{question}")`；要固定文本，才用 `HumanMessage(content="...")`。
- reinforce_prompt: 为什么 `HumanMessage(content="{question}")` 可能让模型收到字面量 `{question}`？
- status: active

### 2026-07-08 03:32 - LangChain Memory - state 的作用

- id: lq-20260708-0332-state-role
- chapter: md/27_LangChain对话记忆.md
- question: LangChain 对话记忆这一章里 `state` 的作用是什么？
- answer_summary: `state` 是应用当前运行流程里需要保存和传递的完整状态包，不只包含聊天历史。`chat history` 只管历史消息，而 `state` 还可以包含 user_id、session_id、messages、检索文档、工具步骤、任务是否完成等信息；本章只需知道它比 memory 更宽，后面 LangGraph 会重点使用。
- exact_rule: `chat history` 只管历史消息；`state` 是更大的运行状态容器，用来在多步 AI 流程里传递中间信息。
- reinforce_prompt: 为什么说 `messages` 只是 `state` 的一部分？
- status: active

### 2026-07-09 08:39 - LangChain Agent - create_agent 和 invoke 参数

- id: lq-20260709-0839-create-agent-invoke-params
- chapter: md/28_LangChain_Agent工具调用.md
- question: `agent = create_agent(model=llm, tools=[search_project_knowledge])` 和 `result = agent.invoke({"messages": [{"role": "user", "content": question}]})` 这两步里的新方法和参数分别是干什么的？
- answer_summary: `create_agent(...)` 是组装阶段，用 `model=llm` 指定模型，用 `tools=[...]` 指定 Agent 可以请求哪些工具，返回一个可反复调用的 Agent 对象。`agent.invoke(...)` 是执行阶段，把本轮输入 state 交给 Agent；其中 `messages` 是消息列表，`role="user"` 表示用户消息，`content=question` 是本次问题。Agent 返回的是包含完整消息状态的 result，不只是字符串。
- exact_rule: `create_agent(...)` 先组装带工具能力的 Agent；`agent.invoke({"messages": [...]})` 再启动一次真实工具调用流程。
- reinforce_prompt: `create_agent` 和 `agent.invoke` 哪个是组装阶段，哪个是执行阶段？
- status: active

### 2026-07-09 08:43 - LangChain Agent - create_agent 来源和引入

- id: lq-20260709-0843-create-agent-import
- chapter: md/28_LangChain_Agent工具调用.md
- question: `create_agent` 是否是真实存在的函数，应该如何引入？
- answer_summary: `create_agent` 是 LangChain 当前环境里真实存在的函数，可以从 `langchain.agents` 引入：`from langchain.agents import create_agent`。它不是 Python 内置函数，也不是项目自己定义的函数，而是 LangChain 提供的 Agent 创建函数；在项目根目录可用 `poetry run python -c "from langchain.agents import create_agent; print(create_agent)"` 验证。
- exact_rule: 用 `from langchain.agents import create_agent` 引入；不确定来源时，用 Poetry 环境执行一次 import 验证。
- reinforce_prompt: `create_agent` 来自哪个包，如何在当前项目验证它能导入？
- status: active

### 2026-07-09 09:21 - LangChain Agent - tools 参数和 TOOLS 的区别

- id: lq-20260709-0921-agent-tools-vs-tools-json
- chapter: md/28_LangChain_Agent工具调用.md
- question: `create_agent(..., tools=...)` 里的 `tools` 是否要放第 26 章写过的 `TOOLS`？
- answer_summary: 本章不要把第 26 章的 `TOOLS` 直接放进 `create_agent`。第 26 章的 `TOOLS` 是给模型看的工具说明书，`TOOL_FUNCTIONS` 是后端执行映射；而 LangChain Agent 的 `tools=[search_project_knowledge]` 通常放真实可执行函数或 `@tool` 包装后的 Tool，LangChain 会用函数名、类型注解和 docstring 自动生成工具说明，并保留执行能力。
- exact_rule: 手写 loop 用 `TOOLS + TOOL_FUNCTIONS`；LangChain Agent 用 `tools=[@tool 包装后的函数]`。
- reinforce_prompt: 为什么 `create_agent(..., tools=...)` 里优先放 `@tool` 函数，而不是第 26 章的 `TOOLS` 列表？
- status: active

### 2026-07-09 11:27 - LangChain Agent - @tool docstring 位置和作用

- id: lq-20260709-1127-tool-docstring-location
- chapter: md/28_LangChain_Agent工具调用.md
- question: `@tool` 装饰器里的 docstring 写在哪里，是不是就当普通注释写？
- answer_summary: docstring 写在函数定义下面的第一行，放在函数体里面，用三引号包起来。它看起来像注释，但不是普通 `#` 注释；普通注释只给人看，而 docstring 会成为函数的 `__doc__`，并且会被 LangChain 的 `@tool` 用来生成模型可见的工具描述。工具 docstring 应该写清楚这个工具什么时候该用。
- exact_rule: `@tool` 函数的 docstring 写在函数体第一行；它既是函数说明，也是模型选择工具时看的工具描述。
- reinforce_prompt: `# 注释` 和 `"""docstring"""` 在 `@tool` 工具里有什么区别？
- status: active

### 2026-07-09 11:35 - LangChain Agent - 原始检索函数和 Tool 包装函数

- id: lq-20260709-1135-core-function-vs-tool-wrapper
- chapter: md/28_LangChain_Agent工具调用.md
- question: 能不能直接给 `search_knowledge_base(query, limit) -> list[Document]` 加 `@tool`，让它作为 Agent 工具？
- answer_summary: 语法上可以，加了 `@tool` 后它会变成 LangChain 的 `StructuredTool`，Agent 能看到工具名、描述和参数。但本章不推荐直接装饰原始检索函数，因为它会改变函数身份，项目其他地方如果还想拿 `list[Document]` 做 RAG 或评估会变别扭；更清楚的分层是保留原始函数返回 `Document`，另写一个 `@tool` 包装函数，把 `Document` 格式化成短字符串给模型。
- exact_rule: 核心检索函数保留 `list[Document]`；Agent 工具函数用 `@tool` 包一层并返回 `str`。
- reinforce_prompt: 为什么不建议直接给原始 RAG 检索函数加 `@tool`？
- status: active

### 2026-07-09 13:37 - LangChain Agent - Document 格式化输出

- id: lq-20260709-1337-document-format-output
- chapter: md/28_LangChain_Agent工具调用.md
- question: `f"[{index}] {doc.page_content[:800]}" for index, doc in enumerate(docs, start=1)` 这段出来的格式是什么？
- answer_summary: `enumerate(docs, start=1)` 会给每个 Document 编号，从 1 开始；`doc.page_content[:800]` 取每个切片正文前 800 个字符；`f"[{index}] ..."` 把编号和正文拼成一条文本。外层如果再用 `"\n\n".join(...)`，就会得到一个用空行分隔的证据字符串，例如 `[1] 第一段正文\n\n[2] 第二段正文`。
- exact_rule: `enumerate` 负责编号，f-string 负责拼每条文本，`join` 负责把多条文本拼成一个工具输出字符串。
- reinforce_prompt: `enumerate(docs, start=1)`、`doc.page_content[:800]`、`"\n\n".join(...)` 分别负责什么？
- status: active

### 2026-07-09 13:40 - Python 基础 - f-string 和模板字符串

- id: lq-20260709-1340-f-string-template-expression
- chapter: md/28_LangChain_Agent工具调用.md
- question: `f"[{index}] {doc.page_content[:800]}"` 里前面的字符串是怎么拿到后面的 `doc` 的，是不是模板字符串？
- answer_summary: 这是 Python 的 f-string，作用类似 JavaScript 的模板字符串。字符串前面加 `f` 后，`{...}` 里面可以写变量或表达式，Python 会先计算 `{index}` 和 `{doc.page_content[:800]}`，再把结果塞回字符串；不是字符串主动去拿后面的值，而是解释器在创建字符串时做表达式替换。
- exact_rule: Python f-string = `f"...{表达式}..."`；JS 模板字符串 = `` `...${表达式}...` ``。
- reinforce_prompt: `f"[{index}] {doc.page_content[:800]}"` 会先计算哪两个表达式？
- status: active
